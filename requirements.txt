# ─────────────────────────────────────────────────────────────────────────────
# ASTL — Adaptive Serving Stack for LLMs
# Requirements for V100-SXM2-32GB  |  CUDA 12.0/12.1  |  Python 3.12
# ─────────────────────────────────────────────────────────────────────────────
#
# Install order matters — run the install script below, NOT just pip install -r:
#
#   # 1. Create venv
#   python3 -m venv /root/Farhoud/.venv
#   source /root/Farhoud/.venv/bin/activate
#
#   # 2. PyTorch (CUDA 12.1 wheel — required before vLLM)
#   pip install torch==2.4.0 torchvision==0.19.0 \
#       --index-url https://download.pytorch.org/whl/cu121
#
#   # 3. vLLM (CUDA 12.1 wheel)
#   pip install vllm==0.6.3 \
#       --extra-index-url https://download.pytorch.org/whl/cu121
#
#   # 4. Pin numpy AFTER vllm (vllm pulls numpy>=2 via other deps)
#   pip install "numpy<2.0.0" --force-reinstall
#
#   # 5. Remaining packages
#   pip install -r requirements.txt --no-deps   # use --no-deps to avoid re-upgrading numpy
#
# After install, apply TWO patches to vLLM config.py for Llama 3.1 rope_scaling:
#   python3 patch_vllm.py   (see patch_vllm.py in this repo)
# ─────────────────────────────────────────────────────────────────────────────

# ── Core ML / Serving ────────────────────────────────────────────────────────
torch==2.4.0
vllm==0.6.3
numpy==1.26.4

# ── Transformers stack ───────────────────────────────────────────────────────
transformers==4.45.2
tokenizers==0.20.3
huggingface-hub==0.36.2
safetensors==0.4.5
accelerate==0.34.2

# ── Datasets ─────────────────────────────────────────────────────────────────
datasets==3.2.0

# ── Classifiers ──────────────────────────────────────────────────────────────
xgboost==1.7.6
catboost==1.2.5
scikit-learn==1.5.2

# ── Data / Utils ─────────────────────────────────────────────────────────────
pandas==2.2.3
joblib==1.4.2
scipy==1.14.1

# ── GPU Monitoring ───────────────────────────────────────────────────────────
pynvml==11.5.0

# ── Async / Networking ───────────────────────────────────────────────────────
aiohttp==3.10.5
uvloop==0.21.0

# ── HuggingFace auth (for gated models: Llama 3.1) ───────────────────────────
huggingface-hub==0.36.2

# ── Misc dependencies pulled by vllm/transformers ────────────────────────────
packaging==24.1
filelock==3.16.1
fsspec==2024.9.0
regex==2024.9.11
tqdm==4.66.5
requests==2.32.3
pyyaml==6.0.2
typing-extensions==4.12.2
